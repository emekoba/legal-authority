import 'package:flutter/material.dart';

class AudioList extends StatelessWidget {
  final Function dismissInput;

  const AudioList({Key key, this.dismissInput}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
